% UNITSFSS
%
% Files
%   BTU            - function BTU = BTU(), return BTU/(ft-lbf) conversion factor
%   celsius2kelvin - function Kelvin = celsius2kelvin(temperature_in_degrees_celsius ),
%   deg            - function deg = deg(), return rad/deg conversion factor
%   degC2degF      - function degF = degC2degF(temperature_in_degrees_celsius ),
%   degC2degK      - function degK = degC2degK(degC)
%   degC2degR      - function degF = degC2degR(temperature_in_degrees_celsius ),
%   degF2degC      - function degC = degF2degC(degF)
%   degF2degR      - function degR = degF2degR(degF)
%   degK           - function degK = degK(), return  conversion factor from deg Kelvin to basic temperature unit (deg
%   degK2degC      - function degC = degK2degC(degK)
%   degR           - function degR = degR(), return deg Rankine to basic temperature unit (also Rankine) conversion factor
%   degR2degF      - function degR = degR2degF(degR)
%   dtr            - function dtr = dtr(), return rad/deg conversion factor
%   fpm            - function fpm = fpm(), return fpm = ft/minute conversion factor
%   fps            - function fps = fps(), return fps = ft/second conversion factor
%   fssbasedunits  - The units.m returns a struct with the FSS (ft, slug, second) values of many common units.
%   ft             - function ft = ft(), return ft/ft conversion factor
%   gal            - function gallon = gal()
%   gravity        - function g = gravity(), return 1g/(basic acceleration unit) conversion factor
%   hour           - function hour = hour(), return hour/(basic time unit) conversion factor
%   hp             - function hp = hp(), return (ft-lbf/s)/hp conversion factor
%   in             - function in = in(), return ft/in conversion factor
%   inch           - function inch = inch(), return ft/inch conversion factor
%   inHg           - function inHg = inHg(), return inHg = inHG/(lbf/ft^2) pressure units conversion factor
%   kg             - function kg = kg(), return sl/kg conversion factor
%   km             - function km = km(), return ft/km conversion factor, feet/kilometer
%   kts            - function kts = kts(), return kts/(basic speed unit) conversion factor
%   kW             - function kW = kW(), return kW/(basic power unit) conversion factor
%   lbf            - function lbf = lbf(), return lbf/(basic force unit) conversion factor
%   lbm            - function lbm = lbm(), return sl/lbm conversion factor
%   meter          - function meter = meter(), return ft/meter conversion factor
%   mi             - function mi = mi(), return ft/mi conversion factor, feet/mile
%   minute         - function minute = minute(), return minute/(basic time unit) conversion factor
%   mph            - function mph = mph(), return mph = miles/hour conversion factor
%   nmi            - function nmi = nmi(), return ft/nmi conversion factor, feet/nautical mile
%   rpm            - function rpm = rpm(), return rpm/(rad/s) conversion factor
%   rtd            - function rtd = rtd(), return rad/deg conversion factor
%   second         - function second = second(), return second/second conversion factor
%   sl             - function slug = sl(), return slug/slug conversion factor
%   slug           - function slug = slug(), return slug/slug conversion factor
%   slugin2        - function slugin2 = slugin2(), return (sl*ft^2) / (sl*in^2) conversion factor
