function lbm = lbm()
% function lbm = lbm(), return sl/lbm conversion factor
% e.g. use,
% for computation Fuel_in_sl = Fuel_in_lbm * lbm   converts from lbm to sl
% for conversion on output such as display
% disp(Fuel_in_sl / lbm)   will convert from units of sl to lbm

lbm = 1/32.2;
